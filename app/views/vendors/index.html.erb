<main>
  <h1>Vendors</h1>
  <ul>
    <% @vendors.each do |vendor| %>
    <li> <%= link_to vendor.username, vendor_products_path(vendor.id) %> Contact Me: <%= vendor.email %></li>
    <%= image_tag vendor.image_url %>
    <% end %>
  </ul>
</main>
