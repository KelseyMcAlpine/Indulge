require "test_helper"

describe Order do
  let(:order) { Order.new }

  it "creates a new order when the first product is added in a browser session" do

  end

  it "does not create a new order when the 2nd and subsequent products are added in the same browser session" do

  end

  it "relates to a product through the order_product table" do

  end

end
