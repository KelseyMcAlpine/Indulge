<h1>Product Detail Page</h1>
<p>
  <ul>
  <li>
    <%= image_tag @product.photo_url %>
  </li>
  <li>
    <%= @product.name %>
  </li>
  <li>
    <%= @product.description %>
  </li>
  <li>
    <%= @product.price %>
  </li>
    <li>
      sold by: <%= link_to @product.vendor.username, vendor_path(@product.vendor.id) %>
    </li>
    Categories:
  </ul>
  <ul>
  <% @product.categories.each do |category| %>
    <li> <%= category.name %> </li>
  <% end %>
  </ul>

</p>
