<div id="header">
  <h3><%= @vendor.username %>: Product Management Page</h3>
  <p> Vendor email: <%= @vendor.email %> </p>
  <h3 class="button rounded addprod"><%= link_to "Add new product", new_product_path %></h3>
</div>


<table class="center">
  <thead>
    <tr>
      <th>Product</th>
      <th>Product Image</th>
      <th>Product Inventory</th>
      <th>Product Description  </th>
      <th>Product Lifecycle </th>
      <th>Change Availability</th>
      <th>Edit Product Details</th>
      <th>Remove Product</th>
    </tr>
  </thead>

  <% @vendor.products.each do |product| %>
  <tr>
    <td> <%= link_to "#{product.name}", product_path(product.id) %></td>
    <td> <img class="prod-img-list" src="<%=product.photo_url%>"></img></td>
    <td> <%= product.inventory %> </td>
    <td> <%= product.description %> </td>
    <td> <%= product.lifecycle %> </td>
    <td>
      <% if product.lifecycle == "available" %>
        <%= link_to "Retire", update_availability_path(product.id), data: { confirm: "Are you sure you want to retire this product?" }, method: :update_availability %>
      <% else %>
        <%= link_to "ReList", update_availability_path(product.id), data: { confirm: "Are you sure you want to re-list this product?" }, method: :update_availability %>
      <% end %>
    </td>
    <td> <%= link_to "Edit", edit_product_path(product.id)%> </td>
    <td> <%= link_to "Remove", product_path(product.id), data: { confirm: "Are you sure you want to delete this product?" }, method: :delete %> </td>
  </tr>
  <% end %>
</table>




  <!-- <% @vendor.products.each do |product| %>
  <div class="block">
      <img class="prod-img-list" src="<%=product.photo_url%>"></img>
      <div>

          <p class="button small ">
            <% if product.lifecycle == "available" %>
              <%= link_to "Retire", update_availability_path(product.id), data: { confirm: "Are you sure you want to retire this product?" }, method: :update_availability %>
            <% else %>
              <%= link_to "ReList", update_availability_path(product.id), data: { confirm: "Are you sure you want to re-list this product?" }, method: :update_availability %>
            <% end %>
          </p>

          <p class="button small">
            <%= link_to "Edit", edit_product_path(product.id)%>
          </p>

          <p class="button small">
            <%= link_to "Remove", product_path(product.id), data: { confirm: "Are you sure you want to delete this product?" }, method: :delete %>
          </p>

    </div>
      <div>
        <%= link_to "#{product.name}", product_path(product.id) %>
        <%= product.description %>
      </div>

      <div>
        <%= product.inventory %>
      </div>

      <div>
        <%= product.lifecycle %>
      </div>


</div>


<% end %> -->
