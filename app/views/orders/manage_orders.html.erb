<!-- <h1>Welcome, <%= Vendor.find_by_id(session[:vendor_id])%>! </h1> -->
<h1>Welcome, <%= Vendor.find(1).username%>! </h1>


<table>
  <tr>
    <th>Order</th>
    <th>Date</th>
    <th>Product</th>
    <th>Price</th>
    <th>Quantity</th>
    <th>Subtotal</th>
    <th>Order Status</th>
    <th>Your Status</th>
    <th>Edit Status</th>
  </tr>
  <% @vendor_products.each do | order | %>
  <tr>
    <td><%= order.order_id %></td>
    <td><%= order.created_at %></td>
    <td><%=order.product_id %></td>
    <td><%=order.price%></td>
    <td><%=order.order_id %></td>
    <td><%=order.order_id %></td>
  </tr>
  <% end %>
</table>
